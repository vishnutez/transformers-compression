# Core HF stack
transformers>=4.36
accelerate>=0.26
datasets>=2.16
tokenizers>=0.15
safetensors>=0.4

# PyTorch (CUDA build should be installed separately via the correct index/conda)
# torch>=2.1
# torchvision>=0.16
# torchaudio>=2.1

psutil>=5.9

# Attention kernels
flash-attn>=2.5.8
triton>=2.1

# Training utilities
numpy>=1.24
packaging>=23.2
pyyaml>=6.0
tqdm>=4.66
wandb>=0.16
